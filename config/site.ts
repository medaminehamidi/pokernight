export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: 'Poker Night',
  description: 'Literal Poker Night(mare)',
  links: {
    signup: '/signup',
    signin: '/signin',
    profile: '/profile',
    home: '/'
  }
}

